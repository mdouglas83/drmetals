This folder stores fallback contact submissions when PHP email delivery is not enabled yet.

If `config.json` keeps `"mail.enabled": false`, `contact.php` will append submissions to `storage/inquiries.log`.
